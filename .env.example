# Copy to .env and set the paths for your machine
#
# FETCHLANE_REPO_PATH — absolute path to the generic-data-access-backend checkout
# (not needed once Fetchlane is published to Docker Hub)
FETCHLANE_REPO_PATH=/path/to/generic-data-access-backend
